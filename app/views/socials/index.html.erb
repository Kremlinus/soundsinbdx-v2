<p id="notice"><%= notice %></p>

<h1>Socials</h1>

<table>
  <thead>
    <tr>
      <th>Label</th>
      <th>Link</th>
      <th>About u</th>
      <th>Artist</th>
      <th>Event</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @socials.each do |social| %>
      <tr>
        <td><%= social.label %></td>
        <td><%= social.link %></td>
        <td><%= social.about_u %></td>
        <td><%= social.artist %></td>
        <td><%= social.event %></td>
        <td><%= link_to 'Show', social %></td>
      </tr>
    <% end %>
  </tbody>
</table>
